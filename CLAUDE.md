# CLAUDE.md — دستور ورشة سيف تداول (اقرأه كاملاً قبل أي سطر)

## من أنت وأين تعمل
أنت ورشة تأليف لمشروع "سيف تداول" — منصة تقييم أسهم السوق السعودي (248 سهماً، نموذج 5 محاور).
الإنتاج يعمل على سيرفر منفصل **لا تراه ولا تصله** — أنت تؤلف محلياً فقط، والنشر يمر عبر بوابة حوكمة خارجية (dry-run + مراجعة ناقد + قرار المالك).

## قواعد غير قابلة للتفاوض
1. **الفرع**: تعمل على `dev-ui` حصراً. ممنوع أي commit أو push على `master` نهائياً.
2. **ملفات البيانات ممنوعة من التعديل**: `stocks-data.json`، `tasi-history.json`، `snapshots/`، وكتلة `__STOCKS_DATA__` أو `const DATA` المحقونة في index.html — تتغير على السيرفر يومياً ونسختك بائتة بطبيعتها.
3. **لا أسرار أبداً**: لا مفاتيح API ولا tokens ولا مسارات سرية في أي ملف أو commit، حتى كمثال.
4. **ملكية الملفات**: ملفك هو `template.html` (وما تنشئه من ملفات جديدة تحته). ملف `watchlist-accuracy.html` **ليس ملكك** — تعمل عليه جهة أخرى بالتوازي، لمسه = تعارض دمج مضمون.
5. **البنية الثابتة مقدسة**: GitHub Pages static — لا SPA، لا frameworks، لا bundlers، لا خطوة build. HTML/CSS/JS خام في ملف واحد.

## خريطة البنية (احفظها)
- `template.html` = **المصدر** الذي تعدله. يحوي علامة `__STOCKS_DATA__` — السيرفر يحقن مكانها JSON كاملاً (~2MB) وينتج `index.html`.
- `index.html` = **مولَّد، لا يُعدل يدوياً أبداً** — لكنه مفيد لك: يحوي بيانات حقيقية كاملة.
- **لبناء بيئة اختبارك المحلية**: استخرج كائن البيانات من index.html (ابدأ raw parse من الموضع بعد علامة الحقن — الكائن أضخم من أن يُلتقط بـ regex)، خذ عينة 20-30 سهماً متنوعة التصنيفات + المفاتيح العلوية (marketRegime, sectorMedians, lastUpdated...) واحفظها `sample-data.json` محلياً، ثم اصنع سكربت اختبار بسيطاً يحقنها في template لتعاين النتيجة بالمتصفح. ملف العينة لا يُدفع للمستودع (أضفه لـ .gitignore).

## حقول البيانات المهمة لكل سهم
`symbol, name, sector, currentPrice, priceUpdatedAt, investmentScore: { total (من 100), classCode (strong_buy|buy|conditional_buy|hold|sell|strong_sell|filtered), classification (عربي), timing (ممتاز|مقبول|انتظر|N/A), details (تفسير كل محور — أساس بطاقة "لماذا"), topDrivers }` — والمفاتيح العلوية: `marketRegime { regime, icon, advice, pctVsSma, breadth }, lastUpdated`.

## أسلوب التسليم
- commits صغيرة ذات رسائل واضحة بالعربية أو الإنجليزية
- كل ميزة تعمل مستقلةً قابلة للمعاينة بفتح الملف محلياً
- عند اكتمال النطاق: ادفع dev-ui وأبلغ المالك — **لا تدمج، لا تنشر**
- قبل التسليم مرّر عملك على وكيل `critic` (في .claude/agents/) وأرفق خلاصة مراجعته

## المرجعية عند التعارض
TASK-BRIEF.md يحدد النطاق. هذا الملف يحدد الحدود. عند أي غموض: **توقف واسأل المالك** — لا تجتهد في المناطق الرمادية.
