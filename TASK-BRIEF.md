# TASK-BRIEF — بند 9: موجة الواجهة 1 (الجزء الإنشائي)

**الملف الهدف**: `template.html` حصراً | **الفرع**: `dev-ui` | **الوكيل القائد**: ux-expert | **المراجع قبل التسليم**: critic

## النطاق — أربع ميزات بالترتيب

### 1. شاشة "قرارات اليوم" (أعلى الصفحة، قبل الجدول)
- بطاقات **أعلى 5 فرص**: الأعلى نقاطاً من classCode ∈ {strong_buy, buy, conditional_buy} — كل بطاقة: الاسم، الرمز، القطاع، النقاط/100، التصنيف بشارته، التوقيت، وأبرز topDrivers
- شريط "**ما الذي تغيّر**": مقارنة بسيطة داخل الجلسة غير متاحة (لا بيانات أمس في الصفحة) — اعرض بدلها: عدد كل تصنيف اليوم + تنبيه إن كان marketRegime هابطاً ("البيئة ضاغطة — الأفضلية للانتظار" من حقل advice)
- بانر نظام السوق القائم يبقى فوق كل شيء كما هو

### 2. فلاتر + بحث فوري
- فلاتر: قطاع (قائمة) | تصنيف (شرائح ملونة قابلة للتعدد) | نقاط (مدى منزلق أو عتبات 80/65/50)
- بحث فوري بالاسم أو الرمز (عربي/أرقام) يعمل مع الفلاتر معاً
- الفلترة على DOM القائم بلا إعادة بناء — الأداء على 248 صفاً ملموس

### 3. بطاقة "لماذا هذا التقييم؟"
- عند ضغط أي صف/سهم: بطاقة تعرض `investmentScore.details` — تفسير كل محور من الخمسة بنقاطه، + topDrivers
- إن غاب details لسهم (7 أسهم من 248): اعرض "التفاصيل غير متاحة لهذا السهم" — لا تخترع تفسيراً

### 4. إخفاء الحقول البائدة
- تُخفى من كل العرض: riskScore, riskLevel, confidence, interpretation (إخفاء فقط — تبقى في البيانات)
- مكانها في الصف يعوضه مدخل بطاقة "لماذا"

## معايير القبول (يفحصها critic ثم بوابة الإنتاج)
1. صفر frameworks، صفر build، كل شيء في template.html
2. **ميزانية الوزن موثقة**: KB المضافة مقابل خطة تعويضها — التعويض الفعلي من الأخبار قد يُنفذ في بوابة الإنتاج، لكن الرقم مطلوب منك
3. جوال: بلا تمرير أفقي إجباري، البطاقات تتكدس، اللمس مريح
4. مجرب محلياً على عينة تشمل: التصنيفات السبعة كلها + أسهم بلا details + نظام هابط
5. RTL سليم بالكامل
6. لا لمس لـ: `__STOCKS_DATA__`، index.html، watchlist-accuracy.html، أي ملف بيانات

## خطوات البدء المقترحة
1. اقرأ CLAUDE.md ولخص القيود للمالك
2. ابنِ بيئة الاختبار المحلية (العينة من index.html كما في CLAUDE.md)
3. نفّذ الميزات 4 ← 2 ← 1 ← 3 (الأسهل فالأصعب) بـ commit لكل ميزة
4. مراجعة critic ← معالجة ملاحظاته ← push إلى dev-ui ← أبلغ المالك

## ما بعد تسليمك (لعلمك فقط — ليس عملك)
الفرع يُسحب على سيرفر الإنتاج بمسار تجريبي ← dry-run بالبيانات الحية ← مراجعة ناقد الإنتاج ← قرار المالك ← النشر بقاعدة الإيقاع (تغيير واحد لكل تشغيلة نظيفة، قد يُقسم عملك على عدة دفعات نشر).
